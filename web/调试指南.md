# 🐛 空白页调试指南

## 第一步：查看浏览器控制台错误

这是**最重要**的一步！

### 如何打开控制台：

1. **Chrome/Edge/Firefox**：按 `F12` 或 `Ctrl+Shift+I` (Windows) / `Cmd+Option+I` (Mac)
2. 点击 **Console** 标签
3. **截图或复制所有红色错误信息**发给我

### 常见错误类型：

#### 错误 1: 模块导入失败
```
Failed to resolve module specifier "@/xxx"
```
**原因：** 路径别名配置问题
**解决：** 检查 `vite.config.ts` 和 `tsconfig.json`

#### 错误 2: 依赖缺失
```
Error: Cannot find module 'xxx'
```
**原因：** 依赖没有安装
**解决：** 运行 `npm install`

#### 错误 3: 语法错误
```
Uncaught SyntaxError: ...
```
**原因：** 代码有语法错误
**解决：** 查看具体错误文件和行号

#### 错误 4: Pinia 错误
```
getActivePinia was called with no active Pinia
```
**原因：** Store 在 Pinia 初始化前被调用
**解决：** 已修复，应该不会出现

## 第二步：检查 Network 标签

1. 打开浏览器 DevTools
2. 点击 **Network** 标签
3. 刷新页面（Ctrl+R 或 Cmd+R）
4. 查看是否有**红色**的请求（404 或 500 错误）

### 应该看到的正常请求：

- `localhost:3000/` - 200 (HTML)
- `/@vite/client` - 200
- `/src/main.ts` - 200
- `/src/App.vue` - 200
- 其他 `.vue` 和 `.ts` 文件 - 200

### 如果有 404 错误：

说明文件路径不对或文件不存在。

## 第三步：最简单的测试

### 测试 1: 修改 index.html，使用简化版本

临时修改 `web/index.html` 第 11 行：

```html
<!-- 原来的 -->
<script type="module" src="/src/main.ts"></script>

<!-- 改成 -->
<script type="module" src="/src/main-simple.ts"></script>
```

保存后刷新浏览器，如果看到 "测试页面" 说明 Vue 本身没问题。

### 测试 2: 检查终端输出

开发服务器终端应该显示：

```
  VITE v5.2.6  ready in 500 ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
```

如果有错误，请复制终端的**完整错误信息**。

### 测试 3: 清除缓存和重新安装

```bash
# 1. 停止开发服务器 (Ctrl+C)

# 2. 删除缓存和依赖
cd /Users/niuhaipeng/code-niuhp/ai-teacher/web
rm -rf node_modules
rm -rf .vite
rm package-lock.json

# 3. 重新安装
npm install

# 4. 重新启动
npm run dev
```

## 第四步：检查环境

### 检查 Node.js 版本

```bash
node -v
# 应该显示: v16.0.0 或更高

npm -v
# 应该显示: v8.0.0 或更高
```

如果版本太低，请更新 Node.js。

### 检查是否在正确目录

```bash
pwd
# 应该显示: /Users/niuhaipeng/code-niuhp/ai-teacher/web

ls
# 应该看到: package.json  src/  index.html  等文件
```

## 第五步：逐步排查导入

如果简化版本能工作，说明问题在某个依赖上。

### 修改 `src/main.ts`，逐步注释掉导入：

```typescript
import { createApp } from 'vue'
// import { createPinia } from 'pinia'  // 先注释掉
// import piniaPluginPersistedstate from 'pinia-plugin-persistedstate'
// import ElementPlus from 'element-plus'
// import 'element-plus/dist/index.css'
// import 'element-plus/theme-chalk/dark/css-vars.css'
// import zhCn from 'element-plus/es/locale/lang/zh-cn'
// import * as ElementPlusIconsVue from '@element-plus/icons-vue'

import App from './App.vue'
// import router from './router'  // 先注释掉
// import './styles/index.scss'  // 先注释掉

const app = createApp(App)

// const pinia = createPinia()
// pinia.use(piniaPluginPersistedstate)

// for (const [key, component] of Object.entries(ElementPlusIconsVue)) {
//   app.component(key, component)
// }

// app.use(pinia)
// app.use(router)
// app.use(ElementPlus, { locale: zhCn })

app.mount('#app')
```

保存，如果页面能显示，说明问题在某个被注释的导入中。
然后逐个取消注释，找出具体是哪个导入有问题。

## 第六步：常见解决方案

### 方案 1: 重新安装 Element Plus

```bash
npm uninstall element-plus
npm install element-plus@latest
```

### 方案 2: 确保所有依赖都已安装

```bash
npm install vue@latest
npm install vue-router@latest
npm install pinia@latest
npm install pinia-plugin-persistedstate@latest
npm install element-plus@latest
npm install @element-plus/icons-vue@latest
```

### 方案 3: 检查 vite.config.ts

确保配置正确：

```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src'),
    },
  },
  server: {
    port: 3000,
  },
})
```

## 🆘 如果以上都不行

请提供以下信息：

1. ✅ **浏览器控制台截图** (Console 标签的所有错误)
2. ✅ **终端输出截图** (运行 npm run dev 后的完整输出)
3. ✅ **Network 标签截图** (刷新页面后的所有请求)
4. ✅ Node.js 版本 (运行 `node -v`)
5. ✅ 操作系统版本

我会根据这些信息帮你解决问题！

## 💡 快速测试命令

在浏览器控制台执行：

```javascript
// 测试 Vue 是否加载
console.log(window.__VUE__)

// 测试 app 是否存在
console.log(document.getElementById('app'))

// 测试是否有 JavaScript 错误
console.log('JS 正常运行')
```

如果这些都能正常输出，说明问题可能在 Vue 组件内部。

